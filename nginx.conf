server {
    listen 8080;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Forwards client-side routes (React Router) back to index.html
        try_files $uri $uri/ /index.html;
    }
}